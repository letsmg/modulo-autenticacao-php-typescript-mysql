{
  "name": "ts",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && vite build && npm run build:iife",
    "build:iife": "npx esbuild dist/login-form.js --bundle --format=iife --global-name=LoginForm --outfile=public/js/login-form.js && npx esbuild dist/user-form.js --bundle --format=iife --global-name=UserForm --outfile=public/js/user-form.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "bootstrap": "^5.3.8",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}